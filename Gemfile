source 'https://rubygems.org'

gemspec

# Keep development dependencies out of Gemfile, since we don't package
# Rakefile, specs, etc in the gem.
gem 'rake', '~> 12.0'
gem 'bundler', '~> 1.13'
gem 'rspec', '~> 3.5'
gem 'simplecov', '~> 0.12', require: false
